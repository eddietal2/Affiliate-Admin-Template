import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Affiliate-Admin-Template',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
